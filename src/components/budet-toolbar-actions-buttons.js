import Image from "next/image";

export default function BudgetToolBarActionsButtons({children}) {
    return (
        <div className="budget-toobar-actions-buttons">
            {children}
        </div>
    )
}
